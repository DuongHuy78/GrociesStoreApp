name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  #comunicate wite API
  dio: ^5.9.0
  #save data in app
  shared_preferences: ^2.5.3
  #generate code in API
  retrofit: ^4.1.0
  #contribute annotation for generating code
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0
  bloc: ^9.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  #generate code
  freezed: ^2.5.2
  json_serializable: ^6.7.1
  retrofit_generator: ^8.1.0
  build_runner: ^2.4.9


flutter:
  uses-material-design: true
  assets:
    - assets/images/